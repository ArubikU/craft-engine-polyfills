# Cycle logic gate mode 0-5 on right-click.
mode = Machine.get_typed("mode", "int")
mode = (mode + 1) % 6
Machine.set_typed("mode", "int", mode)
Machine.play_sound("minecraft:block.stone_button.click_on", 0.5, 1.0)
