# Vertical gearbox — relays rpm via Java pullRotationalPower, toggles activated state for rendering.

def on_break():
    Machine.set_rpm_output(0)
    Machine.report_su(0)
end

# Activation is driven by the machine's `status` field (see the machine JSON).
def status():
    return rpm != 0 ? "true" : "false"
end



